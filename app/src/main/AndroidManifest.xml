<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.unl.fct.campus.firstwebapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>



    <application

        android:name=".LoginApp"

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClosingTheGap"
        android:usesCleartextTraffic="true">

       <activity android:name=".user.Main_Page"></activity>
        <activity android:name=".ui.login.Register"></activity>
        <activity android:name=".ui.login.RegisterOptional"></activity>
        <activity android:name=".ui.login.RegisterOptional2"></activity>
        <activity android:name=".user.Profile"></activity>
        <activity android:name=".user.Settings"></activity>



        <activity
            android:name=".ui.login.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>